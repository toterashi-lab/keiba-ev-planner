# Data Quality Agent

Owns source coverage, raw hashes, parser versions, missing values, runner-set equality, and temporal availability. It can block a race but cannot raise probability or expected return.
